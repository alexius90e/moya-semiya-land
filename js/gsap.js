// gsap.registerPlugin(ScrollTrigger);
// const fruitsImage = document.querySelector('.fruits__image');
// gsap.to(fruitsImage, {

//   scrollTrigger: {
//     trigger: '.story__image',
//     start: 'top bottom',
//     end: 'bottom top',
//     scrub: true,
//     onUpdate: (self) => {
//       if (self.direction > 0) {
//         gsap.to(fruitsImage, { rotation: '+=14', overwrite: 'true' });
//       } else {
//         gsap.to(fruitsImage, { rotation: '-=14', overwrite: 'true' });
//       }
//     },
//   },
// });
